import { Input, Select, Button, Tag, InputNumber } from 'antd'

const { TextArea } = Input;

export default {
  input: Input,
  select: Select,
  button: Button,
  textarea: TextArea,
  tag: Tag,
  inputnumber: InputNumber
}
