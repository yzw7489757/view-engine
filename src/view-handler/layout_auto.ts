import { IViewLayout, IViewData, ViewEngineProps } from '../interface';

function generatorAutoLayout(viewLayout: IViewLayout, viewData: IViewData, props: ViewEngineProps) {
  const layout = []
  

  return layout;
}
export default generatorAutoLayout